# Python packages of the Stage-3 study (checkers, EXP-3 analysis, Jupyter notebook),
# pinned to the versions with which the committed outputs were produced and tested
# (Python 3.14.5, Windows 11).  Install with
#     python -m pip install -r requirements-stage3.txt
# With other versions every check still passes, but the last digits of the EXP-3
# analysis files (fits.json, fits_scan.csv), of the checker reports and the bytes of
# the figures and of the executed notebook can differ from the committed files
# (see the student guide, Sections 3.5 and 14).
numpy==2.4.6
matplotlib==3.11.0
sympy==1.14.0
nbformat==5.10.4
nbclient==0.10.2
ipykernel==7.1.0
