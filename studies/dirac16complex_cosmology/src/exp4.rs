//! EXP-4 -- dirac16complex quanta as dark matter: Fermi-gas EoS and pair creation.
//!
//! STUB.  This file is owned by the agent implementing EXP-4; it must keep
//! exactly the signature below and may use the shared API documented at the
//! top of `src/lib.rs` (RunContext, ExperimentSummary, driver, spinor,
//! output, math, generated constants).  Output directory:
//! `ctx.experiment_dir("exp4")`.

use crate::{ExperimentSummary, RunContext};

/// Run EXP-4 (not implemented yet).
pub fn run(_ctx: &RunContext) -> Result<ExperimentSummary, String> {
    Err("not implemented yet".to_string())
}
