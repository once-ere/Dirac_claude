export const meta = {
  name: 'understand-dirac16-inputs',
  description: 'Parallel readers survey solver repo, dirac-main build machinery, and notebook physics sections',
  phases: [{ title: 'Survey', detail: 'three independent read-only surveys' }],
}

const SP = 'C:/Users/nsh/AppData/Local/Temp/claude/C--Users-nsh-Developer-github-Dirac-claude/cc75e05b-1dc1-4a82-a1dd-e65cb8479099/scratchpad'
const ROOT = 'C:/Users/nsh/Developer/github/Dirac_claude'

const REPORT = {
  type: 'object',
  properties: {
    summary: { type: 'string', description: 'dense multi-paragraph technical summary' },
    key_files: { type: 'array', items: { type: 'object', properties: { path: { type: 'string' }, role: { type: 'string' } }, required: ['path', 'role'] } },
    commands: { type: 'array', items: { type: 'string' }, description: 'exact verified commands (build/run/test) with notes' },
    reusable_code: { type: 'array', items: { type: 'object', properties: { path: { type: 'string' }, what: { type: 'string' }, how_to_reuse: { type: 'string' } }, required: ['path', 'what', 'how_to_reuse'] } },
    pitfalls: { type: 'array', items: { type: 'string' } },
    open_questions: { type: 'array', items: { type: 'string' } },
  },
  required: ['summary', 'key_files', 'commands', 'reusable_code', 'pitfalls', 'open_questions'],
}

const tasks = [
  {
    label: 'solver-repo',
    prompt: `READ-ONLY survey. Do not modify any file outside ${SP}/survey_tmp (you may create that dir for scratch builds only).
A shallow clone of https://github.com/once-ere/rustSolveIt_Win11_SUNDIALS_7_8_0 is at ${SP}/rustSolveIt_Win11 (Windows 11, Git Bash + PowerShell available; cargo, rustc, python 3.14, wolframscript available; jupyter is NOT on PATH).
Goal: we must build a NEW numerical study (stiff ODE systems, ~50-500 real states, from a Dirac-type spinor field coupled to cosmology) that "employs Mathematica notebooks and Jupyter notebooks from" this repo. Determine precisely:
1. Repo layout and purpose of: notebooks/, dynamic_notebooks/, SolveIt_Notebooks_for_rust/, scripts/solveit, sundials_rs/ (crates cvode_rs etc.), datasets/, evidence/.
2. HOW a user solves an ODE with it: the Rust API of cvode_rs (show a minimal real example from the repo with exact function names, e.g. dense BDF Newton, tolerances, user RHS closure, output), any CLI binary (e.g. 'solveit'), any Python binding / Jupyter kernel mechanism (evcxr? subprocess? pyo3?), and any Mathematica interface (LibraryLink, RunProcess, JSON exchange). Quote exact code snippets (short) with file paths.
3. Which notebook(s) are the best templates to adapt for our study (one Mathematica .nb/.wl and one Jupyter .ipynb): what they do cell-by-cell at high level, what kernel/language the ipynb uses, what they read/write, how they call the solver.
4. Try (in ${SP}/survey_tmp only, or with cargo --target-dir ${SP}/survey_tmp/target) to build and run ONE minimal cvode example from the repo to prove the toolchain works on this machine; report the exact commands and whether it succeeded, with timing. If the build is too long (>10 min) stop and report.
5. Licensing/attribution notes (LICENSE files) relevant to copying notebook code into another repo.
6. Check whether ${ROOT}/dirac-main/vendor/sundials_rs (the submodule used by the existing dirac-main studies; it may be empty if the submodule was not initialised) relates to this repo's sundials_rs (same crate names/versions?). Report crate names, versions and the path/git dependency style used by ${ROOT}/dirac-main/Cargo.toml and studies/*/Cargo.toml.
Be exhaustive and concrete; exact paths and commands matter more than prose.`,
  },
  {
    label: 'dirac-main-machinery',
    prompt: `READ-ONLY survey of ${ROOT}/dirac-main (do not modify anything; scratch only under ${SP}/survey_tmp2).
We will add NEW provenance documents (Markdown -> LaTeX -> PDF) and a NEW numerical study to a new repository, reusing dirac-main's machinery. Determine precisely:
1. scripts/build_dissertation_tex.py: its Markdown subset (headings, $$ math $$, inline code, tables, fenced code), flags (--strip-heading-numbers, --input, --output), how it maps to LaTeX, its preamble/packages, known limitations (what markdown breaks it). Run it (output to ${SP}/survey_tmp2) on provenance/EINSTEIN_SPINOR_44.md and confirm pdflatex (MiKTeX) compiles the result warning-free; report exact commands and any warnings.
2. scripts/check_provenance_pdf.py and check_dissertation_pdf.py: what they check (hash pins? page size? deterministic builds, SOURCE_DATE_EPOCH, pdftex settings) and how an '--edition' is registered, so a new edition could be added.
3. The Rust study layout: dirac-main/Cargo.toml workspace, studies/einstein_spinor_44 (Cargo.toml, src/*.rs): how CVODE is called (exact API: builder, tolerances, dense linear solver, RHS signature), how CSV/JSON outputs are written deterministically, how constants are generated by scripts/generate_*_constants.py. Quote the key RHS + solver-setup code.
4. The check_*.py pattern for semantic verification of numerical output (e.g. check_einstein_spinor_44.py): list the categories of checks.
5. wolfram/CurvedSpinGeometry.wl and scripts/verify_curved_spin_geometry.wls: structure and exact conventions (vielbein postulate, omega index placement, D_mu = d + (1/8) omega_{mu ab}[g^a,g^b]); tests/ layout and how unittest discovers them.
6. Notebook generation: scripts/build_jupyter_notebook.py, run_jupyter_notebook.py (how does it execute an ipynb without jupyter on PATH? nbclient? python -m ...?), build_mathematica_notebook.wls, verify_mathematica_notebook.wls.
7. Check whether python packages are available: numpy, scipy, matplotlib, nbformat, nbclient, ipykernel, jupyter (python -m jupyter), sympy, mpmath. Report versions (python -c "import X; print(X.__version__)").
8. Whether dirac-main/vendor/sundials_rs is populated (submodule) and whether 'cargo build --release -p <study>' works offline right now (try one study with --target-dir ${SP}/survey_tmp2/target; stop if >10 min).
Report exact commands, versions and paths.`,
  },
  {
    label: 'notebook-physics',
    prompt: `READ-ONLY. A text dump of a large Mathematica notebook is at ${SP}/nb_clean.txt (base64 image lines removed; long cells truncated at 3000 chars). The FULL untruncated dump is ${SP}/nb_cells.txt (648 MB — use grep -n with -m and cut, never cat it). Original notebook: ${ROOT}/Pair_Creation_of_Universes_WaveFunctionOfUniverse-4+4-Einstein-Lovelock-Nash.nb.
Already understood (do not re-explain): tau/taubar/T16^A/sigma16 construction, SAB, omega-matrix definition, MatrixMetric44 = diag{Cot[6Hx0]^2, Sin[6Hx0]^(1/3) E^(2a4[Hx4]) (x3), -1, -Sin[6Hx0]^(1/3) E^(-2a4[Hx4]) (x3)}, Lg[] definition (cell ~1064) and eL[] Euler-Lagrange derivation treating f16[k] as commuting functions.
Find and report, with cell numbers and verbatim short quotes:
1. The Einstein-Lovelock equations used (which Lovelock orders, coupling constants, 'A_ab = 0' vacuum equations), and whether MatrixMetric44 is claimed to solve them in vacuum or with a source; what equations determine a4 (search 'a4', 'ProductLog', 'DSolve', 'EQ1', 'EQ2', 'solvedEQ'); what solutions for a4 were found (explicit formulas).
2. The energy-momentum / source tensor TU_mu_nu derivation (text cells ~23-28 and any code computing it): the formula used, and any results (e.g. off-diagonal components, 'Lambda g_mu_nu' hope).
3. The mass-eigenvalue / 'M c[j]' / 'pair of universes with masses +-M' argument and any 'superluminal inflation/deflation' solution formulas for Psi16 components (nZ[j], Z[j], yZ[j] solutions such as sin(C Q t) * sin(z)^(+-sqrt(M^2-Q^2)/6)); state the separated equations if shown.
4. Any definitions/values of Q1, Q2, K, K0, K4, H, M, and the coordinate change z = 6 H x0, t = H x4.
5. The 'base16', 'symm16', 'j=134' mass-matrix alternatives and 'covariantDiffMatrix' = T16A[5].T16A[6].T16A[7]: what role they play.
6. Any mentions of Grassmann/anticommuting/fermion/quantization/Dirac sea/canonical anticommutator in the notebook.
7. The notebook's section structure: list all Section/Subsection titles with cell numbers in order (grep '\\[Section\\]' and '\\[Subsection\\]').
Be precise; quote formulas as they appear (InputForm).`,
  },
]

phase('Survey')
const results = await parallel(tasks.map(t => () => agent(t.prompt, { label: t.label, phase: 'Survey', schema: REPORT })))
return tasks.map((t, i) => ({ label: t.label, report: results[i] }))
