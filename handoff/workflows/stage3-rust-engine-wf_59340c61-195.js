export const meta = {
  name: 'stage3-rust-engine',
  description: 'Stage 3: Rust CVODE crate core, then five dark-sector experiments with independent Python checkers',
  phases: [
    { title: 'Core', detail: 'crate skeleton, CVODE driver, generated gammas, CLI, EXP-1, EXP-5' },
    { title: 'Experiments', detail: 'EXP-2, EXP-3, EXP-4 modules plus checkers, in parallel' },
  ],
}

const ROOT = 'C:/Users/nsh/Developer/github/Dirac_claude'
const SP = 'C:/Users/nsh/AppData/Local/Temp/claude/C--Users-nsh-Developer-github-Dirac-claude/cc75e05b-1dc1-4a82-a1dd-e65cb8479099/scratchpad'
const CRATE = 'studies/dirac16complex_cosmology'
const OUT = 'artifacts/dirac16complex/numerics'

const COMMON = `
CONTEXT:
- Repository root ${ROOT} (git repo, nothing committed; do not commit or push). Read-only references: ${ROOT}/dirac-main (published as github.com/once-ere/dirac; see studies/einstein_spinor_44/src/{lib.rs,main.rs} for the house CVODE style and deterministic writers), the pinned solver engine at ${ROOT}/vendor/rustSolveIt (sundials_rs/ and planet_Mercury/notebook/; DO NOT MODIFY vendor/), and ${SP}/rustSolveIt_Win11/planet_Mercury/mercury_rs/src/{driver.rs,rhs.rs,output.rs,main.rs} (production CVODE driver pattern, SUCCESS/FAILURE last-line CLI contract).
- READ FULLY FIRST: ${SP}/CONTRACT.md (physics conventions, binding) and ${SP}/NUMERICS_CONTRACT.md (the five experiments, binding). Also ${SP}/survey_solver-repo.md (API facts: fn-pointer callbacks, user_data Box<dyn Any> + downcast_mut, N_VGetArrayPointer RefMut guard must be dropped before solver calls, fmt_e(v,17) for output, +fma required, closure trampoline pattern) and ${SP}/survey_dirac-main-machinery.md.
- Gamma matrices: the independent Stage-1 builder writes ${ROOT}/artifacts/dirac16complex/arbitrary-field/algebra-fixture.json (keys include "gamma" = 8 integer 16x16 matrices in the notebook split-octonion basis, zero-based, "C", "chirality"). scripts/generate_dirac16complex_constants.py must read it, re-verify exactly (Clifford relations, C = g0 g1 g2 g3, C symmetric, (C g^a)^T = -C g^a, B = -i C g^4 Hermitian with B^2 = I) and emit src/generated.rs with the fixture's sha256 embedded. If the fixture does not yet exist when you start, write the generator anyway, build the matrices from the definition in CONTRACT.md section 1 inside the generator as a fallback, and make the generator REFUSE (exit 1) if the fixture exists and disagrees; rerun it once the fixture exists (poll the file at most every 2 minutes, up to 60 minutes; if still absent, proceed with the fallback and say so).
- Build: from ${ROOT}/${CRATE}: cargo build --release (the repo-root .cargo/config.toml pins +fma; it applies because cargo walks up from the crate dir). Crate: own empty [workspace], edition 2021, license GPL-3.0-or-later, path deps ../../vendor/rustSolveIt/sundials_rs/crates/{sundials_core,cvode_rs}; commit a Cargo.lock; #![forbid(unsafe_code)] #![deny(warnings)]; cargo fmt --check and cargo clippy --all-targets -- -D warnings clean; unit tests with cargo test --release.
- Deterministic outputs: CSV (header row, fmt_e(v,17), LF, trailing newline) and summary.json (fixed key order, 2-space indent, floats via fmt_e(v,17)) under ${ROOT}/${OUT}/<exp>/; running the same command twice must give byte-identical files. Transcendentals via sundials_core::sundials_libm (exp, log, sin, cos, sinh, cosh, atan, ...); pow as exp(b*log(x)); document any function not available (e.g. tanh = sinh/cosh).
- Python checkers: numpy + stdlib only (no scipy). Independent re-derivation: never import the Rust outputs' own derived columns as truth; recompute from the state columns and the physics. Each checker prints check_<name>=true/false, measurement_<name>=..., check_count, failed_check_count and exits nonzero on failure; also writes ${ROOT}/${OUT}/<exp>/python-check-report.json.
- Scratch only under ${SP}/stage3_tmp/<label>/. Be honest: never claim a check passes unless computed; if a physics expectation of NUMERICS_CONTRACT.md turns out false numerically, keep the check, report the measured truth, and explain (do not tune tolerances to hide it).
`

const REPORT = {
  type: 'object',
  properties: {
    files_written: { type: 'array', items: { type: 'string' } },
    commands_run: { type: 'array', items: { type: 'string' } },
    checks_passed: { type: 'array', items: { type: 'string' } },
    checks_failed: { type: 'array', items: { type: 'string' } },
    key_results: { type: 'array', items: { type: 'string' }, description: 'quantitative physics results name=value' },
    contract_problems: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
  required: ['files_written', 'commands_run', 'checks_passed', 'checks_failed', 'key_results', 'contract_problems', 'notes'],
}

phase('Core')
const core = await agent(`${COMMON}
YOU OWN: ${ROOT}/${CRATE}/ (Cargo.toml, Cargo.lock, src/main.rs, src/lib.rs, src/driver.rs, src/spinor.rs, src/output.rs, src/generated.rs, src/exp1.rs, src/exp5.rs, and STUB files src/exp2.rs, src/exp3.rs, src/exp4.rs), ${ROOT}/scripts/generate_dirac16complex_constants.py, ${ROOT}/scripts/check_dirac16complex_exp1.py, ${ROOT}/scripts/check_dirac16complex_exp5.py, ${ROOT}/${OUT}/exp1/, ${ROOT}/${OUT}/exp5/.
TASK:
1. driver.rs: a reusable CVODE driver: integrate(y0: Vec<f64>, t0, targets: &[f64], rhs: Box<dyn FnMut(f64,&[f64],&mut [f64]) -> Result<(),String>>, cfg: SolverConfig{method: Bdf|Adams, rtol, atol (scalar or vector), max_step, max_num_steps, stop_time}) -> Result<Integration{times, states, steps, rhs_evals, ...}, String> using the closure trampoline through user_data, with dense linear solver for BDF (Newton) and functional/fixed-point or Newton for Adams (document the choice), every flag checked, teardown in C order; plus integrate_backward support (targets decreasing).
2. spinor.rs: 16x16 complex helpers on [f64; 512]-like storage (or Vec<(f64,f64)>): gamma^a, C, B, chirality from generated.rs; h(t) builder h = -i M_eff g4 - g4 sum_j (k_j/h_j) g^j; the RHS i du/dt = h u for u in C^16 stored as 32 reals (re0..re15, im0..im15 - document the layout); bilinears u^dag M u; the expectation-value rule <Psi^dag M Psi> = u^dag B M u (NUMERICS_CONTRACT); eigenvector computation for Hermitian h via an exact construction (for k along a single axis you can build eigenprojectors P_pm = (1 +- h/E)/2 and take P_+ applied to basis vectors, Gram-Schmidt, normalise) - no external linear-algebra crates.
3. main.rs CLI: subcommands print-config, exp1, exp2, exp3, exp4, exp5, all; flags --output DIR (default ${OUT}), --rtol, --atol, --refined (tolerances /10 and max_step /2); last stdout line SUCCESS or FAILURE with exit code 0/1. exp2/exp3/exp4 are implemented by OTHER agents in src/exp2.rs, src/exp3.rs, src/exp4.rs: create those files now as stubs exposing exactly "pub fn run(ctx: &crate::RunContext) -> Result<crate::ExperimentSummary, String>" returning Err("not implemented yet"), and define RunContext / ExperimentSummary / shared writer helpers in lib.rs/output.rs so that the other agents need to edit ONLY their own expN.rs file (plus their own checker script). Document this API in a header comment in lib.rs.
4. EXP-1 and EXP-5 fully, exactly per NUMERICS_CONTRACT.md (EXP-1: primordial field, two a4 profiles, K in {0,0.5,2}, several initial spinors, frozen rho/p/KE/PE/w, u^dag u and u^dag B u conservation, Einstein source requirement columns rho_req, p_req,i, scale factors; parameters H=1, m=1, lambda=0 and one run with lambda != 0 (mean-field M_eff = m + lambda S with S from s(u) times the fixed volume factor - it stays constant); EXP-5: extra-time instability, q in {0.05, 0.1}, growth vs WKB rate, Krein norm conserved). Choose time grids, tolerances (e.g. rtol 1e-10, atol 1e-12) and write them in summary.json.
5. Checkers check_dirac16complex_exp1.py and _exp5.py: independent numpy re-derivation (rebuild gammas from the fixture, recompute h, rho, pressures, KE/PE, invariants from the CSV state columns; five-point finite-difference residual of the ODE on the CSV samples; analytic solution comparison: for constant M_eff the exact solution is u(t) = exp(-i h t) u0 with h constant - compare; frozen-ness; a4-profile independence (the two profiles' spinor columns identical to 1e-12); EXP-5 growth vs WKB within a stated tolerance and Krein norm drift < 1e-8), repeat-run byte identity (run the binary twice into two dirs and compare, done by the checker when given --repeat DIR) and refined-tolerance convergence (--refined DIR).
6. Run everything; all checks must pass (or be reported honestly). cargo test must include at least: Clifford relations from generated.rs, h Hermitian in the good sector, energy eigenvalues +-sqrt(m^2+k^2), exact-solution comparison for a constant h.`, { label: 'core+exp1+exp5', phase: 'Core', schema: REPORT })

log('core done: ' + (core ? core.checks_failed.length + ' failed checks' : 'NO RESULT'))

const expAgents = [
  { label: 'exp2', owns: 'src/exp2.rs, scripts/check_dirac16complex_exp2.py, ' + OUT + '/exp2/', extra: 'EXP-2 exactly per NUMERICS_CONTRACT.md: self-consistent 8D Einstein (kappa_8=1) + mean-field dirac16complex, homogeneous anisotropic (b,a,c), three runs x0 in {0,-0.4,+0.5}, initial H_a=1,H_c=-0.2,H_b=0 with S_0 fixed by the constraint (solve it: kappa*rho(S_0)=3H_bH_a+3H_bH_c+3H_a^2+3H_c^2+9H_aH_c with rho = m S (1 + x0) at S=S_0, m=1), forward to a time where all H_i have converged and backward as far as regular; state (ln b, ln a, ln c, H_b, H_a, H_c, u in C^16); S = S_0 (V_0/V) s(u). Columns: all states, V, Theta, S, rho, p, w, KE_L, PE_L, KE_H, PE_H, constraint residual, bound Theta^2-3H_a^2-2kappa*rho, w_eff for a 3-space observer, s(u). Checker: constraint preserved (<1e-9 relative), anisotropy (H_i-H_j)*V constant, S*V constant, rho-dot + Theta(rho+p)=0 by finite differences, Einstein evolution equations by finite differences, the bound >= 0 everywhere, analytic late-time behaviour, repeat + refined.' },
  { label: 'exp3', owns: 'src/exp3.rs, scripts/check_dirac16complex_exp3.py, ' + OUT + '/exp3/', extra: 'EXP-3 exactly per NUMERICS_CONTRACT.md: 4D-effective FRW with Omega_r=0.00009, Omega_m=0.305, Omega_psi=0.69491, condensate S propto a^-3 obtained from the evolving spinor (reduced frequency mu in {3,7}: results for rho, p, w must be mu-independent to solver precision - check), integrated in N=ln a from 0 down to ln(1/3.5) and up to ln 2 (two branches joined, present once), for x0 in {-0.462654 (w0=-0.861), -0.433107 (w0=-0.764), -0.3, -0.2, 0}. State: u=H0 t, comoving distance D_C (dD_C/dN = 1/(a E)... derive carefully: dD_C/dz = 1/E, with z = 1/a - 1), spinor (32 reals), ln sigma. Columns: N, a, z, E, q (deceleration), rho_psi, p_psi, w, KE_L, PE_L, Omega_psi(a), mu(z)=5 log10((1+z) D_C)+const (H0-free, drop const or fix it), sigma from spinor vs analytic a^-3. Summary: the zeros of rho_psi (a=|x0|^(1/3)), phantom crossing (a=(2|x0|)^(1/3)), tangent CPL (w0, wa = -dw/da at a=1). The CPL FITS (least squares on w(a) over a in [1/3.26,1] and distance-modulus fits of CPL and constant w over z in [0.01,2.26], Nelder-Mead in numpy) and the fine x0 scan belong in the Python checker/analysis script scripts/analyze_dirac16complex_exp3.py (you own it too), writing '+OUT+'/exp3/fits.json; ALSO compute the Unite CPL model (w0=-0.861, wa=-0.60) own distance modulus and its best constant-w projection, and the deflation-index variant gamma (sigma = a^(-3(1-gamma))) that would reproduce the Unite tangent (w0,wa), with the objections noted in fits.json. Checker: E(0)=1 closure, analytic w(a), rho(a) vs closed form, mu-independence, sigma vs a^-3, finite-difference ODE residuals, repeat + refined.' },
  { label: 'exp4', owns: 'src/exp4.rs, scripts/check_dirac16complex_exp4.py, ' + OUT + '/exp4/', extra: 'EXP-4 exactly per NUMERICS_CONTRACT.md: (a) thermal Fermi gas of dirac16complex quanta in a radiation-dominated 3-space (b=c=1), m=1, T_i=10, H_i=0.05 (a=(t/t_i)^(1/2), t_i=1/(2H_i)), a from 1 to at least 50, momentum grid (Gauss-Legendre on [0,k_max], k_max=12 T_i, >=48 nodes; compute the nodes/weights in Rust and write them), for each k one positive-energy eigenvector integrated (plus a second, different spin state for a few k to prove spin-independence), epsilon=u^dag h u, p_1=-(k/a) u^dag g4 g1 u (k along x1; the isotropic 3-space pressure is p=(1/3) times the sum over the three directions = p along the momentum /3 averaged: derive and document), |beta_k|^2 (weight on the instantaneous negative-energy subspace), then rho(a), p(a), w(a), KE_H, PE_H summed with Fermi-Dirac weights f=1/(exp(E_i/T_i)+1) and 16 states per k (8 particles + 8 antiparticles, antiparticle contribution equal by charge-conjugation symmetry - verify for one k by integrating a negative-energy mode). Choose Adams or BDF by testing; high-k modes oscillate many times: verify accuracy by refined tolerances. (b) pair creation: de Sitter 3-space phase (H_inf=1, a=exp(t) for t<0) glued C1 to radiation a=(1+2t)^(1/2) for t>0; vacuum initial state at t0 with k/a(t0)=200 H_inf... (choose so that the mode is adiabatic initially; document), m in {0, 0.1, 0.5, 1, 2}, integrate until k/a and m both >> H (adiabatic), |beta_k|^2 spectrum on a k grid, produced number density n a^3 = (16/(2 pi^2)) Int k^2 |beta_k|^2 dk (state the counting), m=0 gives |beta|^2 <= 1e-10. Checker: kinetic-theory integrals rho=(16/(2pi^2 a^3))Int k^2 f E dk etc. compared with the mode sums (max rel dev), w: early ~1/3, late -> 0, |beta|^2 < 1e-6 in (a), unitarity u^dag u = 1, m=0 no production, spin-independence, repeat + refined.' },
]

phase('Experiments')
const exps = await parallel(expAgents.map(e => () => agent(`${COMMON}
The crate core already exists (written by another agent; read ${ROOT}/${CRATE}/src/lib.rs header comment for the API). Core agent report: ${JSON.stringify(core ? { files: core.files_written, notes: core.notes, problems: core.contract_problems } : 'missing')}.
YOU OWN ONLY: ${ROOT}/${CRATE}/${e.owns.split(', ')[0]}, and ${e.owns.split(', ').slice(1).map(p => ROOT + '/' + p).join(', ')}${e.label === 'exp3' ? ', ' + ROOT + '/scripts/analyze_dirac16complex_exp3.py' : ''}. Do not edit other crate files; if the core API is insufficient, work around it inside your module (you may add private helpers there) and describe the needed change in contract_problems.
TASK: ${e.extra}
Build with cargo build --release, run the binary subcommand (${e.label}) into ${ROOT}/${OUT}/${e.label}/, run your checker (including --repeat and --refined runs into scratch dirs), iterate until everything passes or report honestly. Other experiment agents build the same crate concurrently: if cargo reports a lock/busy target dir or a compile error in another agent's module, use your own --target-dir ${SP}/stage3_tmp/${e.label}/target and retry; never edit their files.`, { label: e.label, phase: 'Experiments', schema: REPORT })))

return { core, exps: expAgents.map((e, i) => ({ label: e.label, report: exps[i] })) }
