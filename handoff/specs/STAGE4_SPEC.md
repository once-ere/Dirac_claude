# Stage 4 — Kohn–Sham DFT for dirac16complex in the primordial gravitational field

Binding together with CONTRACT.md (incl. §11 errata), STAGE2_SPEC.md as corrected by
the Stage-2 reports, and NUMERICS_CONTRACT.md conventions (expectation-value rule
⟨Ψ†MΨ⟩ = u†BMu for one-particle states; number density operator Ψ†BΨ, so the number
density of a mode is u†u ≥ 0; scalar density u†BCu; energy u†Bhu... use the rule).
Measured truth (exact computation) overrides this text; record deviations.

## 1. The static primordial field and its two extensions

Notebook field (Stage 2) with a4 CONSTANT (= a4_0; a4' = 0): this is the static
member of the primordial family and is required for a stationary ground state.
Proper hidden-space coordinate y := ln(sin z)/(6H) ∈ (-∞, 0] on the notebook patch
z ∈ (0, π/2); z = π/2 ↔ y = 0 (cot z = 0: the notebook chart ends, det g → 0 in x0).
    ds² = dy² − dx4² + e^{2Hy} [ e^{2a4_0}(dx1²+dx2²+dx3²) − e^{-2a4_0}(dx5²+dx6²+dx7²) ]
Warp W(y) = e^{Hy}; proper 7-volume element per coordinate volume: W⁶ = e^{6Hy}
(the transverse space pinches off at the "tip" y → −∞, z → 0).  Verified (sympy):
R = −42H²; G^μ_ν = diag(15,15,15,15,21,15,15,15)H² (order y,x1,x2,x3,x4,x5,x6,x7);
required 8D-Einstein source ρ_req = −21H²/κ (negative), p_req = −15H²/κ... (record the
exact mixed components and signs: T^μ_ν = G^μ_ν/κ, ρ = −T^4_4, p_i = T^i_i).
Extrinsic curvature of y = const: K^i_j = H δ^i_j on the six warped directions, 0 on x4.
Two extensions beyond y = 0 (both to be documented; the KS problem uses the Z2 one):
 (E1) smooth: W = e^{Hy} for all y ∈ ℝ (the ζ chart of Stage 2 continued);
 (E2) Z2 mirror ("pair of universes", the notebook's hypothesis): W = e^{−H|y|},
      a brane at y = 0 with Israel stress S^i_j ∝ (K jump): derive exactly with a fixed
      sign convention (record it): magnitude (10,10,10,12,10,10,10)H/κ pattern.
      Structural remark (label as structural, not physical): the γ⁸ map Ψ → γ⁸Ψ sends
      L_{m,U} → −L_{−m,−U} (CONTRACT errata E2), so a mirror copy carrying γ⁸Ψ has
      mass −m: the "±M pair" of the notebook.

## 2. Sector, ansatz and the reduced Kohn–Sham equation

Good sector: Ψ independent of x5,x6,x7 (extra-time momenta q = 0; q ≠ 0 modes are
non-Hermitian/unstable — Stage 1, EXP-5).  3-space: a coordinate torus of size ℓ,
k⃗ ∈ (2π/ℓ)ℤ³.  Stationary in x4.  Ansatz
    Ψ = e^{−iεx4} e^{ik⃗·x⃗} W(y)^{−3} χ(y),      W^{−3} = e^{−3Hy},
which removes the spin-connection term γ^μΩ_μ = 3Hγ⁰ (Stage 2) exactly.  Then
    γ⁰ χ' + i κ(y) k_j γ^j χ − i ε γ⁴ χ = M_eff(y) χ,   κ(y) := e^{−Hy − a4_0},
i.e. χ' = γ⁰ [ M_eff(y) χ − i κ(y) k_j γ^j χ + i ε γ⁴ χ ].  k_j γ^j confines toward the
brane y = 0 (κ grows toward the tip).  Measure: Ψ†Ψ √|g| d⁸x = χ†χ dy d⁷x (flat in y).
Rotational symmetry: the spectrum depends on |k⃗|; compute with k⃗ = (k,0,0).
VERIFIED (numerically, to be re-derived exactly): γ⁰, γ⁰γ¹, γ⁰γ⁴, B and C are
simultaneously block-diagonal in eight 2×2 blocks (common eigenbasis of the commuting
operators J = γ⁰γ¹γ⁴ (J² = 1), γ²γ³ and γ⁵γ⁶); the algebra generated by γ⁰,γ⁰γ¹,γ⁰γ⁴
is 8-dimensional.  Hence the KS equation is a 2×2 first-order system in y with an
8-fold degeneracy (the 8 blocks may split into inequivalent types — determine exactly,
give the explicit basis change and the 2×2 matrices, and the per-block forms of B, C).

## 3. Interaction, Hartree term, exchange and the pseudo-potential

Interaction U(S) = (λ/2) S², S = Ψ̄Ψ (scalar density).  Mean field (Hartree):
M_H(y) = λ S_p(y) with the PROPER scalar density S_p = e^{−6Hy} Σ_{n,k⃗,σ} f(ε) χ†BCχ / ℓ³.
Exchange (Fock) of the contact interaction: VERIFIED for a filled shell of the uniform
gas (8 states at one k⃗): Tr(BCρ BCρ) = S²/8, i.e. E_x = −E_H/8.  Derive the general
finite-T uniform-gas exchange energy density e_x(n, T) (and its scalar/vector
decomposition) of the 8-fold-degenerate relativistic gas with the contact interaction,
exactly (double momentum quadrature; T=0 closed form if possible), including the
antiparticle (Dirac-sea) subtraction convention (normal ordering), and the
corresponding LDA potentials v_s = ∂e_x/∂S (mass-type) and v_v = ∂e_x/∂n (potential-
type); state exactly which combination the KS equation uses (recommended: vector
potential v_x(y) = ∂e_x/∂n at (n_p(y), T), entering as ε → ε − v_x(y) locally; plus the
exact Hartree mass shift).  No correlation term (contact interaction beyond HF is not
renormalizable in 8D; say so).  THE "Kohn–Sham fermion-gas thermodynamics
pseudo-potential" := the local potential pair (M_eff(y) = m + λS_p(y), v_x(y; n_p, T))
built from the thermodynamics of the uniform dirac16complex gas (Mermin finite-T
functional: F = E_KS − T S_entropy + E_H + F_x^{LDA}), plus the gravitational terms
(3Hγ⁰ absorbed by W^{-3}; κ(y) k·γ; boundary conditions).

## 4. Boundary conditions and parameters

At y = 0 (brane, Z2): parity conditions Ψ(−y) = ±γ⁰Ψ(y) (both parities computed;
verify that they make the y-current χ†(Bγ⁴γ⁰ or the correct current matrix)χ vanish at
y = 0).  At y = −L (tip cutoff): a bag condition that kills the y-current (state it in
the 2×2 form; verify).  Report L-dependence (L ∈ {2,3,4}/H) — the tip is a genuine
singular end (W⁶ → 0) and the density there is regularised by L.
Units H = 1.  m/H ∈ {1, 3}.  a4_0 = 0 (plus one check that a4_0 = 0.5 is the rescaling
k → k e^{−a4_0}).  ℓ such that Δk = 2π/ℓ = 0.25 m.  Particle numbers N (closed shells
determined from the spectrum; include at least N = 8 (one shell), one mid-size and one
large ~10³ where LDA is meaningful).  Dimensionless coupling λ̂ = λ m⁶ chosen so that
|λ S_p|/m ranges 0.1–1 in the ground state: λ̂ ∈ {0, +λ̂₁, −λ̂₁, +λ̂₂, −λ̂₂} (report the
values).  T/m ∈ {0, 0.1, 0.3, 1}.

## 5. Kohn–Sham self-consistency and observables

SCF: solve the 2×2 eigenproblem for each k⃗ shell and parity (Rust: CVODE shooting in y
from −L to 0 with the eigenvalue found by bisection/secant on the matching condition;
Python reference: matrix method on a Chebyshev or doubler-free finite-difference grid),
fill N states by ε (Fermi–Dirac at T, chemical potential μ by bisection), rebuild
S_p, n_p, M_eff, v_x, mix (linear/Anderson), iterate to ‖Δn‖ < 1e-10.  Record histories.
Observables: ε_{n}(k,±) spectrum; profiles n_c, n_p, S_p, M_eff, v_x, |χ|² vs y (and
vs z); E_0(N), μ, F(T), E(T), S_entropy(T), C_V(T); first excited state: (i) KS gap
ε_LUMO − ε_HOMO and the particle-hole excitation list, (ii) ΔSCF: self-consistent
constrained-occupation state with one particle promoted (E_1 − E_0), (iii) at finite T
the excitation spectrum; EMT of the KS state in the primordial field (Stage 2
formulas): ρ(y), p_y, p_3 (3-space), p_t (extra-time), proper-volume averages, w's,
brane-localised fraction of the density; comparison with the source the field requires
(ρ_req = −21H²/κ < 0 vs the positive KS energy — state the mismatch quantitatively);
convergence in L, Δk, grid; determinism (repeat) and refined-tolerance runs.

## 6. Deliverables (relabelled for Stage 4)

wolfram/Dirac16ComplexKohnSham.wl, scripts/verify_dirac16complex_kohn_sham.wls (exact:
geometry+brane, reduction to 2×2 with explicit basis, current matrices and BCs,
exchange coefficient/e_x formula checks, KS functional derivatives, EMT reduction);
scripts/check_dirac16complex_kohn_sham_theory.py (independent sympy);
studies/dirac16complex_kohn_sham (Rust, CVODE shooting + SCF; subcommands print-config,
spectrum, scf, excited, thermo, emt, all; deterministic CSV/JSON under
artifacts/dirac16complex/kohn-sham/);
scripts/ks_reference_solver.py (independent numpy solver) and
scripts/check_dirac16complex_kohn_sham.py (checker: reference vs Rust, virial/
Hellmann–Feynman-type identities, current conservation, N conservation, SCF
stationarity, repeat/refined);
notebooks/dirac16complex_kohn_sham.ipynb (+ builder/runner/checker),
notebooks/Dirac16ComplexKohnSham.nb (+ builder/verifier; NDSolve/NDEigensystem
cross-check); figures under artifacts/dirac16complex/kohn-sham/figures/;
provenance/DIRAC16COMPLEX_KOHN_SHAM_PRIMORDIAL.{md,tex,pdf} (all ideas and results),
provenance/DIRAC16COMPLEX_KOHN_SHAM_STUDENT_GUIDE.{md,tex,pdf} (complete numerical
documentation for a student); tests/test_d16c_kohn_sham*.py;
scripts/verify_stage4_kohn_sham.{ps1,sh}; README status update.

## 7. ERRATA from the Stage-2 review (binding; override §1 and §5 where they conflict)

E4.1 The claim "the dirac16complex condensate cannot supply the source the primordial
field requires" is FALSE in general and must not be repeated.  Exact result (Stage-2
document, Section 13.7, verified in both verifiers): for the homogeneous state
Psi = u(x4) (K = -3iH) with a4'' = 0, G_mu_nu = kappa T_mu_nu holds in all 64
components iff (i) the three-gamma bilinears V_a (and W_ij if a4' != 0) vanish,
(ii) m S = -36 H^2/kappa, (iii) lambda S^2 = 2 H^2 (15 - 3 a4'^2)/kappa.  Examples
(H = kappa = 1): a4 = sqrt5 t, lambda = 0, m = 5, S = -36/5, rho = -36, p = 0;
the notebook's a4 = t: m = -15, lambda = 25/6, M_eff = -5, S = 12/5, rho = -24,
p = 12, w = -1/2.  Any a4'' != 0 is excluded because T^i_i = T^j_j = L_s for every
Psi(x0,x4) while G^i_i - G^j_j = 2 H^2 a4''.  For the STATIC field (a4' = 0) the
conditions read m S = -36 H^2/kappa and lambda S^2 = 30 H^2/kappa (so lambda > 0 and
m S < 0: either m < 0 or S < 0).  Stage 4 must therefore ask, quantitatively, whether
the Kohn-Sham ground state (or the first excited/ΔSCF state) can meet these two
conditions for some (m, lambda, N), and report the answer with the sign of S in the
KS state (the scalar density of a positive-energy KS orbital is +u^dagger B C u > 0 for
m > 0, so the sourcing solutions live in the m < 0 / negative-energy sector; discuss
the gamma^8 map m -> -m explicitly).
E4.2 In the Stage-2 document the quantity "K" of the zeta plane wave is real; the
homogeneous state corresponds to K = -3iH (imaginary) - keep these distinct.

## 8. ERRATA from the Stage-4 exact theory (wolfram-kohn-sham-report.json, 125/125; binding)

E4.3 p_req = +15 H^2/kappa in all seven transverse directions (not -15); rho_req = -21 H^2/kappa.
E4.4 Degeneracy: at fixed k = (k,0,0) each level of the block Hamiltonian is 4-fold (the
     four j = +1 blocks); the four j = -1 blocks carry the NEGATED spectrum
     (h_{-1} - v_v = -(h_{+1} - v_v)).  8-fold degeneracy holds at k = 0 and over closed
     shells {k, -k}.  The k = 0 chiral zero mode (chi = (e^{My}, 0), eps = 0) splits as
     +-c k between the two j-types, with c > 0 given in closed form in the report.
E4.5 The y-current matrix after the expectation rule is A4 = gamma^0 gamma^4 (B is
     absorbed); the parity conditions are chi_2(0) = 0 / chi_1(0) = 0 per block; the
     chiral-bag family (1 - Q(theta)) chi(-L) = 0, Q = cos(theta) sigma3 + sin(theta) sigma2.
E4.6 The reflection Psi(-y) = +-gamma^0 Psi(y) is a symmetry of the reduced equation only
     for an ODD mass function (the +-M mirror pair); the even-mass symmetry is
     P_B = i gamma^0 gamma^8, which couples block pairs.  Use the block-level conditions
     of E4.5 as the boundary conditions; discuss both parities as boundary conditions,
     not as symmetries.
E4.7 Exchange is exactly local: e_x = -(lambda/32)(n^2 + S^2) for every T (angular
     average kills p.q); v_v = -(lambda/16) n, v_s = -(lambda/16) S; per block
     -(lambda/4) sum (n^2 + s^2 - t^2 - c^2).  The "exchange-table" is therefore
     unnecessary: implement the closed form (and keep the table only as a cross-check).
     Block ODE: chi' = [M_eff sigma3 - kappa k sigma2 + i j (eps - v_v) sigma1] chi,
     h_j = j[-i sigma1 d_y + M_eff sigma2 + kappa k sigma3] + v_v.
